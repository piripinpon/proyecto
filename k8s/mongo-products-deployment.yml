apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-products
  namespace: proyectom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-products
  template:
    metadata:
      labels:
        app: mongo-products
    spec:
      containers:
        - name: mongo-products
          image: mongo
          ports:
            - containerPort: 27017
