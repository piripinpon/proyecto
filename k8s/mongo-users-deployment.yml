apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-users
  namespace: proyectom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-users
  template:
    metadata:
      labels:
        app: mongo-users
    spec:
      containers:
        - name: mongo-users
          image: mongo
          ports:
            - containerPort: 27017
